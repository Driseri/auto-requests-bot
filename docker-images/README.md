# Docker Images

This folder is for local Docker image archives, for example:

```powershell
docker save alfa-auto-requests-bot:<tag> -o docker-images/alfa-auto-requests-bot-<tag>.tar
```

The archives are ignored by Git via `*.tar` and should not be committed.
